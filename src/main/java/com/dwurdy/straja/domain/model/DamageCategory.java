package com.dwurdy.straja.domain.model;

/** Named damage-policy inputs kept separate from their configured outcomes. */
public enum DamageCategory {
    SECOND_WEAPON_HIT,
    ORDINARY,
    NON_WEAPON,
    EXCEPTIONAL
}
