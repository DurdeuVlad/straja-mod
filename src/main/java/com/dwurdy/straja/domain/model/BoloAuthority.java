package com.dwurdy.straja.domain.model;

/** Display classification only; authority comes from the linked task/order. */
public enum BoloAuthority {
    INFORMATION_ONLY,
    BRING_FOR_QUESTIONING,
    ARREST_AUTHORIZED
}
